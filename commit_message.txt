feat(triggers): implementar disparadores configurables por porcentaje exacto de batería y hora exacta programada

- Agregar soporte para disparador de batería con porcentaje exacto personalizado (`BATTERY_LEVEL:XX`):
  - Integrar panel interactivo en `ShortcutTriggerSection` con Slider dinámico (5% a 100%), visor numérico destacado y chips rápidos (15%, 20%, 50%, 80%, 90%, 100%).
  - Procesar broadcasts de `ACTION_BATTERY_CHANGED` en `PowerTriggerReceiver` con filtrado inteligente, mapeo de atajos por porcentaje y debounce para evitar ejecuciones repetidas.
- Agregar soporte para disparador de hora exacta programada (`TIME_EXACT:HH:mm`):
  - Integrar panel interactivo en `ShortcutTriggerSection` con visor visual de hora, integración nativa con `TimePickerDialog` de Android y chips rápidos de horas habituales (07:00, 08:30, 14:00, 20:00, 22:30).
  - Programar y cancelar alarmas exactas vía `AlarmManager` en `TimeSchedulerHelper` al guardar/editar atajos desde `ShortcutEditorManager`.
  - Ejecutar y reprogramar diariamente los atajos directos en `TimeTriggerReceiver`.
  - Reprogramar automáticamente las alarmas de atajos y automatizaciones tras el reinicio del sistema en `BootReceiver`.
- Actualizar badges contextuales en las tarjetas de atajos (`ShortcutCard`) para reflejar `⏰ HH:mm` y `🔋 XX%`.
- Mejorar el diálogo de nueva automatización (`NewAutomationDialog`) con integración de `TimePickerDialog`.
