feat(triggers): mejorar precisión de alarmas y permisos de segundo plano

- Integra el permiso especial `SYSTEM_ALERT_WINDOW` ("Mostrar sobre otras aplicaciones") en `AndroidManifest.xml`, `OverlayPermissionHelper` y el diálogo modal informativo `OverlayPermissionDialog` para autorizar la apertura de juegos y aplicaciones en segundo plano desde atajos y automatizaciones.
- Corrige en `AutomationsManager.kt` la asignación del `savedId` retornado por `repository.saveAutomation(automation)` para garantizar que las alarmas temporales se programen y cancelen con su ID real persistido en SQLite.
- Actualiza `TimeSchedulerHelper.kt` implementando `AlarmManager.setAlarmClock` con `AlarmClockInfo` como nivel de máxima prioridad del sistema operativo, evitando retrasos inducidos por Doze Mode, optimización de batería o modos de juego.
- Incorpora la tarjeta de gestión y verificación de superposición sobre otras apps en `SettingsScreen.kt`.
- Elimina recursos drawables no utilizados (`flurix_logo.webp`, `flurix_app_icon_1787028976462.jpg`, `ic_shortcuts_app.jpg`) reduciendo el peso de recursos del APK.
- Actualiza la documentación técnica del proyecto (`README.md`, `ROADMAP.md`, `STRUCTURE.md`, `AI_CONTEXT.md` y `Chanelog-beta.md`), indicando que la licencia actual es temporal y preparando la futura transición a una licencia de código abierto (Open Source).
- Añade el workflow de GitHub Actions `foundation.yml` para validaciones de arquitectura, pruebas unitarias y compilación Foundation de calidad en `LuisAlejandro544/Flurix`.
