fix(engine): solucionar inicialización de TextToSpeech en hilo principal y compatibilidad de eventos de carga

- Corregir la inicialización de `TextToSpeech` forzando su creación en el `Main Looper` (`Dispatchers.Main`) para garantizar el enlace IPC con el servicio de voz del sistema en segundo plano.
- Implementar fallback sonoro con `RingtoneManager` y notificación del sistema ante posibles retardos en el motor TTS durante la pantalla bloqueada o segundo plano.
- Agregar soporte a variantes de acciones de sistema (`POWER_CONNECTED` y `POWER_DISCONNECTED`) en `PowerTriggerReceiver` y en el manifiesto para máxima compatibilidad con capas de personalización OEM.
- Activar por defecto la regla de automatización de conexión de cargador en `DefaultShortcutsProvider`.
- Rediseñar el banner de estado de ejecución (`ExecutionStatusBanner`) con proporciones compactas y elegantes.
- Limitar el historial de ejecuciones a 20 registros con purga automática en SQLite Room (`ExecutionLogDao`).
