feat(tiles,triggers): añadir Quick Settings Tile y disparadores automáticos de batería

- Implementar `ShortcutTileService` (`TileService` de Android) para permitir la ejecución inmediata del atajo favorito o más reciente desde el panel de Ajustes Rápidos (Quick Settings Tile) en la cortina de notificaciones o pantalla de bloqueo.
- Diseñar y registrar el icono vectorial `@drawable/ic_qs_tile` para el mosaico del sistema.
- Expandir el motor de disparadores nativos (`ShortcutTrigger`) con soporte para eventos de batería:
  - Batería baja (<15% `ACTION_BATTERY_LOW`).
  - Batería restablecida a nivel seguro (`ACTION_BATTERY_OKAY`).
  - Carga completa al 100% (`BATTERY_FULL`).
- Actualizar `PowerTriggerReceiver` para despachar eventos de energía y batería de forma asíncrona (`goAsync()` en `Dispatchers.IO`).
- Añadir opciones e iconos correspondientes en `ShortcutTriggerSection` dentro del editor visual de atajos.
- Añadir insignias visuales dinámicas (`🪫 Batería baja`, `🔋 Batería OK`, `🔋 Batería 100%`) en `ShortcutCard`.
- Incorporar plantillas preconfiguradas en la Galería: "Modo Ahorro: Alerta Batería Baja" y "Aviso de Carga Completa (100%)".
- Actualizar documentación del proyecto (`README.md`, `ROADMAP.md`, `STRUCTURE.md`, `AI_CONTEXT.md`).
