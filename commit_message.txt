refactor: modularizar AutomationsScreen, MainTabContent, MainActivity y AddActionBottomSheet

- Extraer componentes modulares en `ui/components/automations/` (`AutomationCard.kt`, `NewAutomationDialog.kt`) para desacoplar responsabilidades y reducir el tamaño de `AutomationsScreen.kt`.
- Modularizar los ítems de selección de acción creando el componente reutilizable `ui/components/ActionTypeItem.kt`.
- Desacoplar el paso de eventos entre `MainActivity.kt` y `MainTabContent.kt` mediante contratos tipados en `ui/navigation/NavigationContracts.kt` (`MainTabActions`, `HomeTabActions`, `AutomationsTabActions`, etc.).
- Actualizar `STRUCTURE.md` con la nueva topología de submódulos y delegaciones de interfaz.
- Mantener la integridad de sincronización continua y compatibilidad para distribución en tiendas de terceros y APK directo.
