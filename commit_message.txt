fix(ci): corregir error de sintaxis sh en action de simulacion de dispositivo

- Extraer script de pruebas de hardware y emulación a un archivo ejecutable Bash dedicado (`.github/scripts/simulate_device.sh`):
  - Solucionar error `/usr/bin/sh: 1: Syntax error: end of file unexpected (expecting "fi")` originado por la incompatibilidad de operadores de concatenación `+=` y escapes de cadenas multilínea en el shell POSIX (`dash`/`sh`) utilizado por `reactivecircus/android-emulator-runner`.
  - Asegurar ejecución limpia en entorno `bash` con control estricto de errores (`set -e`) y manejo de argumentos de entrada (`API_LEVEL`, `TEST_BATTERY`).
- Optimizar el flujo `.github/workflows/android-device-simulation.yml`:
  - Añadir paso de preparación con permisos de ejecución (`chmod +x .github/scripts/simulate_device.sh`).
  - Simplificar la invocación dentro del emulador pasando parámetros dinámicos directos.
  - Mantener la generación de resúmenes en Markdown (`$GITHUB_STEP_SUMMARY`) y preservación de logs como artefacto descargable (`android-device-simulation-logs`).
